services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - members_data:/data
    environment:
      - ADMIN_PASSWORD=LaPalette
      - SECRET_KEY=change-this-to-a-random-string
      - DATABASE=/data/members.db

volumes:
  members_data:
